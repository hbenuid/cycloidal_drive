[project]
name = "cycloidal-drive"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cadquery>=2.7.0",
    "numpy>=2.4.2",
    "ocp-vscode>=3.4.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
